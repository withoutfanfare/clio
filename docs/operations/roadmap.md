# Clio Operational Roadmap

This is the canonical register for unfinished deployment, installation and
infrastructure work. The [deployment runbook](deployment.md) explains how to
operate the current system; this file records what still needs to change.

## Keeping the roadmap current

- Review it after every Atlas deployment and on the first working day of each
  month.
- Use only `Planned`, `In progress`, `Blocked` or `Done` as statuses. A blocked
  item must state the condition that will unblock it.
- When work starts, add its issue or pull request link to the status cell and
  keep the roadmap ID in that issue or pull request.
- Mark an item `Done` only when its "Finished when" evidence has been checked.
  Move completed rows to a `Completed` section rather than deleting them.
- Update the corresponding importance-5 Clio project memory after changing
  this file. Git remains the source of truth if the two disagree.

Priority 1 is the next resilience or usability work. Priority 2 becomes useful
as releases, hosts or operational reliance increase.

## Open work

| ID | Priority | Status | Work | Trigger or next action | Finished when |
|---|---:|---|---|---|---|
| CLIO-OPS-001 | 1 | Planned | Off-host Atlas backups | Implement before relying on Atlas for disaster recovery. | Encrypted standalone SQLite snapshots leave Atlas automatically; retention is enforced; failures alert; and a restore drill passes. |
| CLIO-OPS-002 | 1 | Blocked | Install the current release on the Mac mini and MacBook Pro | Resume as soon as each machine is online and reachable by SSH. | Each machine checks out the same pushed SHA, runs `macos-install.sh`, reconnects its AI clients, and passes cross-machine write, recall and namespace checks. |
| CLIO-REL-002 | 1 | Planned | Remove the workstation-only UI package registry dependency | Complete before building the app on another clean Mac or in CI. | `npm ci` installs the pinned private `@stuntrocket/ui` release on a clean authorised machine without copying a developer's Verdaccio storage or committing credentials. |
| CLIO-SEC-001 | 1 | Planned | Update vulnerable UI build dependencies | Use the authorised private package workflow to update Vite, PostCSS, Picomatch and Brace Expansion without sending the private dependency graph to a public audit endpoint. | `npm audit` reports no high or critical findings, the production UI build passes, and the signed Tauri app and DMG package successfully. |
| CLIO-OPS-003 | 1 | Planned | Manage Atlas infrastructure as code | Raised to priority 1 on 2026-07-30: Clio's schedule is now hand-managed in a crontab that Ansible already partly owns, so a playbook run can silently remove it. Scope is enumerated in the Things task "Bake Clio's Atlas configuration into Ansible" — both cron entries, the healthcheck binary, the Slack webhook and capture key as Vault secrets, the `sqlite3` package, the tuned settings file, ONNX Runtime, log rotation and backup retention. | A private Ansible setup recreates the user, packages, Rust toolchain, SSH and firewall policy, ONNX Runtime, both cron entries, the healthcheck and the backup job without committing secrets; a rebuild is proven; and removing the Clio cron entries by hand is detected by CLIO-OPS-005. |
| CLIO-REL-001 | 2 | Planned | Publish release artefacts from CI | Implement when releases become frequent or more than a few Macs need updates. | CI publishes checksummed Linux `x86_64`, macOS `arm64` and macOS `x86_64` builds; the Mac app is Developer ID signed, notarised and stapled; Atlas migration remains an explicit operator step. |
| CLIO-OPS-004 | 2 | Planned | Extend monitoring beyond background liveness | Background work and database integrity are now covered by CLIO-OPS-005. What remains uncovered: disk capacity, backup age and deployment failures. | Disk capacity, backup age and deployment failures are monitored, alert to Slack on a state change, and each alert path has been fired deliberately at least once. |
| CLIO-OPS-006 | 2 | Planned | Give Clio a dedicated Slack channel and consider workflow integration | Alerts currently go to the shared `SLACK_ALERT_WEBHOOK_URL` borrowed from the trader app, which mixes Clio's operational noise into an unrelated channel. Do this before adding more alert types under CLIO-OPS-004. | A Clio-specific webhook is configured via Ansible, alerts land in a dedicated channel, and a decision is recorded on whether to push further into the workflow — for example surfacing pending review-queue items, capture failures needing attention, or a daily digest of what was learned. |
| CLIO-OPS-007 | 2 | Planned | Remove links pointing at archived memories | 55 such links exist after the 2026-07-30 prune (23 inbound, 32 outbound). The auto-linker only creates links, never removes them, so archiving leaves them behind. Linked recall is supposed to honour archive visibility; that has not been tested. | Auto-links to or from archived memories are removed, a test proves linked recall never surfaces an archived memory, and the auto-linker or a maintenance job prevents the backlog recurring. |
| CLIO-OPS-008 | 1 | Blocked | Make session capture outage-safe and recover retained dead letters | Atlas runs `5e56565f55ffc22403950e1dfcbe40672f9edc2a`; all 487 historical dead-letter keys now have exact checkpoints; SQLite `quick_check` is clean; and an isolated patched-hook canary confirmed against Atlas. The validated package passes 38 tests. The restricted workspace denied the Mac installer before mutation, so the installed CLI and hook package remain old. Unblock by running `target/complete-ops008-local-install.sh` outside the sandbox, then restart AI clients and verify one normal Stop-hook capture. | Transient backend failures remain pending with shared backoff; deleted-worktree jobs retain their namespace context; stale gaps recover through exact-once checkpoints; every retained or reconstructed job has a checkpoint; and a live canary succeeds. |
| CLIO-OPS-009 | 1 | In progress ([audit](audits/2026-08-29-memory-and-namespace-audit.md)) | Repair stale memories and namespace fragmentation without rewriting history | Atlas repair is complete: 103 projections archived, 84 memories moved and 193 invalid automatic links removed atomically with a retained backup and rollback journal. Activate pushed Waypoint commit `007a9c7` in the installed desktop/CLI build, then verify one canonical active projection and one archived-projection retirement. | A validated online backup is tied to the approved manifest; the 103 obsolete projections are archived; the approved namespace families are moved without changing semantic timestamps; invalid automatic links are removed but human links remain; the immutable rollback journal commits atomically; post-repair recall/namespace/link canaries pass; and Waypoint no longer recreates repository-slug namespaces or active projections for archived worktrees. |
| CLIO-QUAL-001 | 2 | Planned | Tune the scoring parameters | `scoring.decay_lambda` (0.01) and `scoring.access_boost_weight` (0.1) have never been tuned and directly affect recall ranking. `scripts/bench/recall-eval.py` can now score them the same way it scored the linking settings. | Both parameters are chosen from measured retrieval quality rather than defaults, with the result and method recorded in `scripts/bench/README.md`. |

## Completed

| ID | Completed | Work | Evidence |
|---|---|---|---|
| CLIO-OPS-005 | 2026-07-30 | Liveness signal and Slack alerting for background work | `clio-healthcheck` runs hourly at `:47`, thirty minutes after each auto-link pass. It checks auto-link freshness, that its own cron entry still exists, SQLite integrity, live-memory and link counts, and capture-queue dead-letter and backlog counts where a spool is present. Alerts fire only on a state change, so a persistent fault does not repeat and recovery is reported once. All five paths were exercised on Atlas: healthy run silent; `--test` alert delivered; a forced stale-log failure detected and alerted; the same failure suppressed on repeat; recovery reported. Slack receipt confirmed by the operator. Closes the failure mode that left auto-linking dead for roughly a week in July with no signal. |
| CLIO-COST-001 | 2026-07-30 | Reduce Clio's OpenAI spend and verify the reduction | Measured on the provider dashboard: 947 requests and \$8.93 the day before, against 67 requests and \$1.43 the day after — an 84% cost reduction and a 93% fall in request volume. Changes: capture model GPT-5.6 Terra to GPT-4.1; `consolidate.auto_threshold` 10 to 50; `MAX_DIGEST_CHARS` 80,000 to 24,000; `MAX_ATTEMPTS` 8 to 4; and a skip gate for session deltas with no human input. Clio also moved onto its own API key, which is what made the spend attributable. |
| CLIO-QUAL-002 | 2026-07-30 | Tune auto-linking against measured retrieval quality | `scripts/bench/` added: `link-invariants.sh` (13 checks, including a provably binding total-degree cap), `recall-eval.py` (hold-one-out retrieval scoring, paired arms) and `dial-in.sh` (sweeps one variable at a time). Indicative 2026-07-30 run over 110 queries: briefs went from 2.3 results at 33.2% precision without links to 7.7 at 59.6% with them. Caveats recorded in `scripts/bench/README.md` and the 30 July review: the relevance ground truth is a tag proxy correlated with the embeddings, the baseline query degenerates towards a self-lookup, and the sweep's run-to-run noise (the same configuration measured twice, 0.6pp apart) exceeds every difference between the candidate settings — so threshold 0.60 / cap 5 are **plausible, not evidence-confirmed**; re-run over ≥3 seeds before tuning further. `exclude_kinds` `["receipt"]` rests on the separate degree measurement (receipts averaged 4.86 links vs 2.03 for facts) and stands. |
| CLIO-APP-001 | 2026-07-29 | Persist one Atlas route for CLI, hooks, MCP clients and Tauri | Runtime commit `1934155499535afe7807409f8706cf393174b8a5` is active on Atlas and installed on the MacBook Air. Direct CLI recall, semantic search, the Claude session-start hook and a fresh MCP initialise plus `memory_stats` call all reached Atlas. Copilot and Claude Desktop contain the generated SSH bridge, Kilo was unchanged, the local daemon is disabled, and a Finder-launched signed app displayed `atlas · Connected`. |
