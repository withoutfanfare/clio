# archive — reference only

Local contract for archived material. Inherits repo-wide rules from the root
CLAUDE.md.

`python-prototype/` is the original Python implementation, superseded by the
Rust workspace — reference only. **Do not extend, build, or import from it.**
New work belongs in `crates/`.
